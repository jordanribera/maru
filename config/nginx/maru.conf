upstream api {
  server api:8000;
}

upstream web {
  server api:3000;
}

server {
  listen 9001;
  charset utf-8;

  location = /favicon.ico {
    return 204;
    access_log off;
    log_not_found off;
  }

  location /media/ {
    autoindex on;
    alias /media/;
  }

  location /api/ {
    proxy_pass http://api;
  }

  location / {
    proxy_pass http://web;
  }
}

