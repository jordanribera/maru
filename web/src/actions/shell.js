export const activateTab = (value) => ({
  type: "ACTIVATE_TAB",
  value,
});

export const setDarkMode = (value) => ({
  type: "SET_DARKMODE",
  value,
});

export const setThemeColor = (value) => ({
  type: "SET_THEMECOLOR",
  value,
});
